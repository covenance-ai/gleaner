{
  "description": "Gleaner - harvest Claude Code session transcripts to central storage",
  "hooks": {
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 ${CLAUDE_PLUGIN_ROOT}/scripts/upload-session.py",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
